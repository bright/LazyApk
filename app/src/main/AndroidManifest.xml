<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="pl.brightinventions.lazyapk" >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".LazyApkApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/AppTheme.Up"
            android:name=".ProjectDetailsActivity"
            tools:ignore="UnusedAttribute">
        </activity>
        <activity
            android:theme="@style/AppTheme.Up"
            android:name=".teamcity.TeamcityBuildDetailsActivity"
            tools:ignore="UnusedAttribute">
        </activity>
        <activity
            android:theme="@style/AppTheme.Up"
            android:name=".sources.SourcesActivity"
            tools:ignore="UnusedAttribute">
        </activity>
    </application>

</manifest>
